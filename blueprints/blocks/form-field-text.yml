name: Text
icon: ✏️
fields:
  extends: fields/form-field-basic
  min_length:
    extends: fields/form-field-min-length
    width: 1/6
  max_length:
    extends: fields/form-field-max-length
    width: 1/6
  default:
    extends: fields/form-field-default
    type: text
    width: 4/6
  placeholder:
    extends: fields/form-field-placeholder
    width: 1/2
  pattern:
    extends: fields/form-field-pattern
    width: 1/2
